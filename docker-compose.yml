version: "3.9"

services:
  backend:
    build:
      context: ./backend
    container_name: screenplay-backend
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
    container_name: screenplay-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped