# Artifacts

Versioned generated outputs (documents, tables, media, packages) live here once
the generic artifact subsystem (MM-007) lands. MM-001 establishes the directory
so later packages reuse one store instead of inventing parallel paths.

Do not commit secrets or private screenplay content.
